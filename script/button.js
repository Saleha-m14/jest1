function buttonClick() {
    document.getElementById("par").innerHTML = "You clicked";
}

module.exports = buttonClick;
// how to know that the content of paragraph actually change when we
// don't want to run the html file and only run the javascript
